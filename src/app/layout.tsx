import './globals.css'
import { Roboto } from 'next/font/google'

import { Footer, Header } from '@/layouts'

const roboto = Roboto({
  subsets: ["latin"],
  weight: ["400", "500", "700"],
  variable: "--font-roboto",
})

export const metadata = {
  title: 'My personal portifolio',
  description: 'Generated by create next app',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={`${roboto.variable} font-roboto h-screen bg-white`}>
        <div className='overflow-x-hidden w-screen h-screen'>
          <div className="w-[1440px] h-auto mx-auto mb-8 bg-transparent">
            <Header />
            {children}
          </div>
          <Footer />
        </div>
      </body>
    </html>
  )
}
