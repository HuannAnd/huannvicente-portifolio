version: '3'

services:
  mongodb:
    image: mongo 
    container_name: mymongodb
    restart: always
    ports:
    - '27017:27017'
    # volumes:
    # - ./data/db:data/db
    environment:
    - MONGO_INITDB_ROOT_USERNAME=huannvicente15
    - MONGO_INITDB_ROOT_PASSWORD=amor34312782